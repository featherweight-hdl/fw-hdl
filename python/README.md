# fw-hdl

Featherweight-HDL Python tooling: map fw-hdl SystemVerilog to the Zuspec IR and
synthesize it to synthesizable Verilog RTL.

```
fw-hdl synth <files...> --top <component> --top-module <module> -o out/
```

See `fw/hdl/DESIGN.md` and `fw/hdl/PLAN.md` for the architecture and build plan.
